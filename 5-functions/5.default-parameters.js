// Default parameters: give a parameter a fallback if the caller omits it.
// Once you set a default, every parameter after it should also have a default.

// function interest(principal, rate = 3.5, years = 5) {
//   return ((principal * rate) / 100) * years;
// }

// console.log(interest(10000));
// console.log(interest(10000, 3.5, 5));

// Try it:
// - Call interest with only principal
// - Override rate but keep the default years
