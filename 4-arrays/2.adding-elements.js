// Adding elements: push (end), unshift (start), splice (middle).

const numbers = [3, 4];

// numbers.push(5, 6);           // end
// numbers.unshift(1, 2);        // start
// numbers.splice(2, 0, "a", "b"); // at index 2, delete 0, insert

// console.log(numbers);

// Try it:
// - Push two numbers, then unshift two more
// - Use splice to insert a value in the middle
