// Reducing: reduce boils an array down to a single value.

const numbers = [1, -1, 2, 3];

// let sum = 0;
// for (let n of numbers) sum += n;

// const sum = numbers.reduce(function (accumulator, currentValue) {
//   return accumulator + currentValue;
// }, 0);

// console.log(sum);

// Try it:
// - Sum [1, -1, 2, 3] with reduce
// - Start the accumulator at 0 and log accumulator each step (add a console.log inside)
