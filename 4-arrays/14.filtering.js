// Filtering: filter returns a new array of elements that pass the test.

const numbers = [1, -1, 2, 3];

// const filtered = numbers.filter(function (value) {
//   return value >= 0;
// });
// console.log(filtered);

// Try it:
// - Filter to keep only positive numbers
// - Rewrite the callback as an arrow function
