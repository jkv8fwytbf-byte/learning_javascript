// Spread operator: ... copies elements out of an array (into a new one).

const first = [1, 2, 3];
const second = [4, 5, 6];

// const combined = [...first, "a", ...second, "b"];
// const copy = [...combined];

// console.log(combined);
// console.log(copy);

// Try it:
// - Combine first and second with spread
// - Make a copy of an array and change the copy — does the original change?
