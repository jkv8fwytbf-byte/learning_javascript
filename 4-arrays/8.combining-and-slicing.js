// Combining and slicing: concat joins arrays; slice copies a portion.

const first = [1, 2, 3];
const second = [4, 5, 6];

// const combined = first.concat(second);
// const slice = combined.slice(2, 4); // from index 2 up to (not including) 4

// console.log(combined);
// console.log(slice);

// Try it:
// - concat first and second, then slice the middle two values
// - slice() with no args — what do you get?
