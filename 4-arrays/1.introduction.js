// Arrays: an ordered list of values, accessed by index (starting at 0).

const numbers = [3, 4];

console.log(numbers);
console.log(numbers[0]);
console.log(numbers.length);

// Try it:
// - Log the first and last element
// - Add a third number by index and log .length
