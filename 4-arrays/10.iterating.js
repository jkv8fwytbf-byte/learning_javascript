// Iterating: walk every element with for...of or forEach.

const numbers = [1, 2, 3];

// for (let number of numbers) {
//   console.log(number);
// }

// numbers.forEach(function (number) {
//   console.log(number);
// });

// numbers.forEach((number, index) => console.log(index, number));

// Try it:
// - Log each number with for...of
// - Use forEach to log index and value together
