// Arrays: an ordered list of values. Index starts at 0.

const fruits = ["apple", "banana", "cherry"];

console.log(fruits);
console.log(fruits[0]); // first
console.log(fruits[fruits.length - 1]); // last
console.log(fruits.length);

// Try it:
// - Log the middle element
// - Change fruits[1] and log the whole array
