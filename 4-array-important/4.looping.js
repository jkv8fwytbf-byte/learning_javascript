// for...of walks every element. Use this as your default loop.

const numbers = [10, 20, 30];

for (let number of numbers) {
  console.log(number);
}

// Try it:
// - Loop and log only numbers greater than 15
// - Sum the numbers with a for...of loop (use a let total = 0)

// ponytail: skipped forEach — add when you need the index in a callback
