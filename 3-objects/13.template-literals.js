// Template literals: strings with backticks `` — can embed ${expressions}.

const name = "Siddharth";

// const oldWay = "Hi " + name + "!";
// const better = `Hi ${name}!`;

// const email = `Hi ${name},

// Thank you for joining.

// Regards,
// Sidd`;

// console.log(better);
// console.log(email);

// Try it:
// - Rewrite a "Hello " + name string using a template literal
// - Make a multi-line string with backticks (no \n)
