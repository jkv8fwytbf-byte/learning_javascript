// Operators do something with values (operands).
// Example: in 2 + 3, 2 and 3 are operands, + is the operator.

let x = 10;
let y = 3;

console.log(x + y);
console.log(x > y);
console.log(x === 10);

// Try it:
// - Change x or y and run again: node 01-basics/operators/1.operators.js
